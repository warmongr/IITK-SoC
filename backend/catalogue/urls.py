from django.conf.urls import include, url
from .views import *
# urlpatterns = [
#     url(r'^$',BookView.as_view({'get':'list','post':'create'})),
#     url(r'^book/$',Bookview2),
#     url(r'^book/<int:pk>/',Book_detail),
# ]
